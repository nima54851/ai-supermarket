{
  "name": "Git Automation Workflow",
  "nodes": [
    {
      "name": "Scheduled Trigger",
      "type": "n8n-nodes-base.scheduleTrigger",
      "position": [250, 300],
      "parameters": {
        "rule": {"interval": [{"field": "day", "hours": 9}]}
      }
    },
    {
      "name": "Repo Health Check",
      "type": "n8n-nodes-base.function",
      "position": [500, 300],
      "parameters": {
        "functionCode": "const { execSync } = require('child_process');\nconst repoPath = '/root/.openclaw/workspace/projects/agent-studio';\ntry {\n  const out = execSync(`python3 ${repoPath}/skills/git-automation/repo_health.py --repo ${repoPath}`).toString();\n  return [{json: {health: out}}];\n} catch(e) {\n  return [{json: {health: e.stdout || e.message}}];\n}"
      }
    },
    {
      "name": "Check Staged Changes",
      "type": "n8n-nodes-base.function",
      "position": [750, 300],
      "parameters": {
        "functionCode": "const { execSync } = require('child_process');\nconst repoPath = '/root/.openclaw/workspace/projects/agent-studio';\ntry {\n  const diff = execSync('cd ' + repoPath + ' && git diff --cached', {encoding:'utf8', timeout: 5000});\n  return [{json: {hasChanges: diff.length > 0, diff}}];\n} catch(e) {\n  return [{json: {hasChanges: false}}];\n}"
      }
    },
    {
      "name": "Generate Commit Message",
      "type": "n8n-nodes-base.function",
      "position": [1000, 300],
      "parameters": {
        "functionCode": "const { execSync } = require('child_process');\nconst repoPath = '/root/.openclaw/workspace/projects/agent-studio';\ntry {\n  const diff = execSync('cd ' + repoPath + ' && git diff --cached', {encoding:'utf8', timeout: 5000});\n  if (!diff.trim()) return [{json: {message: null, skip: true}}];\n  const msg = execSync(`echo '${diff.replace(/'/g, \"'\\\\''\" )}' | python3 ${repoPath}/skills/git-automation/commit_message.py --stdin`, {encoding:'utf8', timeout: 10000});\n  return [{json: {message: msg.split('To use:')[0].trim(), skip: false}}];\n} catch(e) {\n  return [{json: {message: null, skip: true, error: e.message}}];\n}"
      }
    },
    {
      "name": "Create PR (if applicable)",
      "type": "n8n-nodes-base.httpRequest",
      "position": [1250, 300],
      "parameters": {
        "method": "POST",
        "url": "https://api.github.com/repos/nima54851/agent-studio/pulls",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {"name": "Authorization", "value": "token {{GITHUB_TOKEN}}"},
            {"name": "Accept", "value": "application/vnd.github+json"}
          ]
        },
        "bodyParameters": {
          "parameters": [
            {"name": "title", "value": "=Auto PR: Daily Update"},
            {"name": "head", "value": "daily-ops"},
            {"name": "base", "value": "main"},
            {"name": "body", "value": "=Generated by agent-studio daily ops"}
          ]
        }
      }
    }
  ],
  "connections": {
    "Scheduled Trigger": { "main": [[{"node": "Repo Health Check", "type": "main", "index": 0}]] },
    "Repo Health Check": { "main": [[{"node": "Check Staged Changes", "type": "main", "index": 0}]] },
    "Check Staged Changes": { "main": [[{"node": "Generate Commit Message", "type": "main", "index": 0}]] },
    "Generate Commit Message": { "main": [[{"node": "Create PR (if applicable)", "type": "main", "index": 0}]] }
  },
  "active": false,
  "settings": {},
  "id": "git-automation-workflow"
}
