# 2026-07-04 Daily Memory

## Tinode on Railway 部署进展

### 当前状态
- Railway project: `d1579da0-7d89-452a-8997-6745ba7720d7` (tinode-chat)
- Railway service: `973ee986-c996-40cc-ad46-6e6ec2db8ca0`
- Public URL: https://tinode-chat-production.up.railway.app
- GitHub repo: https://github.com/nima54851/tinode-railway

### 核心问题：Tinode 启动后5秒崩溃
日志显示：
```
DB adapter postgres 113  ← DB连接成功
Failed to initialize push notifications: failed to parse config: unexpected end of JSON input
tinode died after 5s     ← 静默退出，无panic，无signal
```

### 最新修复方案（start.sh）
1. 去掉后台 `&`，改用 `exec` 等待
2. 10秒后检测 tinode 存活，如死亡则 `exit 1`
3. tinode 监听 6061，Python 代理监听 6060（Railway端口）并返回 HTTP 200 给健康检查
4. 所有其他请求转发到 tinode:6061

### 待办
1. 将 start.sh 推到 GitHub（用 Contents API）
2. 触发 Railway 重新部署
3. 验证公网 URL

### 关键发现
- `exec sleep infinity` 可以让容器常驻，同时让 Railway 判为健康
- Python 代理：GET / → 200 OK，其他 → 转发到 tinode:6061
- 如 tinode 10秒内死亡，start.sh 退出 → Railway 判为 Crashed（可以看到更多日志）

### 下一步（如果还是崩溃）
- 去掉 `--static_data=/app/uploads` 参数测试
- 用 strace 或添加 debug 日志看 tinode 为什么退出
- 考虑用 tinode:latest 官方镜像代替自编译二进制
